struct ProgressViewModel {
    let navigationTitle = "Progress"
    let title = "Keep the progress surface lightweight"
    let message = "Progress will eventually show trends, photo check-ins, and consistency snapshots without turning into a data wall."
    let highlights = [
        "Separate tab placeholder for future progress modules",
        "Clear seam for later data and storage work",
        "No glow score, charts, or trend logic implemented yet"
    ]
    let buttonTitle = "Progress shell ready"
    let buttonSystemImage = "chart.line.uptrend.xyaxis"
}
