struct RoutinesViewModel {
    let navigationTitle = "Routines"
    let title = "Reserve space for repeatable routines"
    let message = "Morning, evening, and custom appearance-related routines can plug into this tab later without changing the root shell."
    let highlights = [
        "Dedicated feature boundary for routine flows",
        "Ready for future routine state, tracking, and reminders",
        "No persistence or habit logic added in Stage 1"
    ]
    let buttonTitle = "Routines shell ready"
    let buttonSystemImage = "checkmark.circle"
}
