struct SettingsViewModel {
    let navigationTitle = "Settings"
    let title = "Keep settings ready for later integrations"
    let message = "Account, subscription, privacy, and notification settings can be layered in later while the tab boundary stays stable."
    let highlights = [
        "Environment already contains auth and subscription service placeholders",
        "Feature boundary is ready for future settings rows and flows",
        "No auth, paywall, or analytics behavior added in Stage 1"
    ]
    let buttonTitle = "Settings shell ready"
    let buttonSystemImage = "gearshape"
}
