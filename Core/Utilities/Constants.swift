enum AppConstants {
    enum Shell {
        static let appName = "Glow App"
        static let foundationTitle = "Stage 1 foundation"
        static let foundationMessage = "The shell keeps routing, dependency injection, and placeholder surfaces in place so later stages can add onboarding and real feature logic without refactoring the app root."
    }
}
